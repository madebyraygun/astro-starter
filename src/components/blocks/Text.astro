---
import { renderMarkdown } from "../../lib/markdown";
const { body, align, background } = Astro.props;
---
<div
  class:list={[
    "block-text",
    align && `align-${align}`,
    background && `block-bg-${background}`,
  ]}
  set:html={renderMarkdown(body)}
/>
