---
import Picture from "../Picture.astro";

const { image, alt, caption } = Astro.props;
---

<figure class="block-image">
  {image && <Picture src={image} alt={alt ?? ""} />}
  {caption && <figcaption>{caption}</figcaption>}
</figure>
